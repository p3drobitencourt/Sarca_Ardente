// app/loading.tsx

import { Loader } from "@/components/ui/loader";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  // For this example, we'll use the centered loader.
  return <Loader />;
}
